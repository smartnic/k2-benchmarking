1) .config here replaces the .config in the Linux kernel root, with the necessary permissions to compile/run eBPF programs.
2) syscall_nrs.c replaces its equivalent file in /samples/bpf with the addition of line 6. Without this addition, running the makefile in samples/bpf gave a warning that was treated as an error.
